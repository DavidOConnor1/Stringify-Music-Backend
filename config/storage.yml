# config/storage.yml
test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# For production, use local disk (simplest)
production:
  service: Disk
  root: <%= Rails.root.join("storage") %>
  
# Alternative: Use Railway's volume if needed
# production:
#   service: Disk
#   root: /data/storage